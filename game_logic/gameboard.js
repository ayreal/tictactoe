const gameBoard = (() => {
  const contents = ["x", "x", "x", "x", "x", "x", "x", "x", "x"];

  const render = () => {
    return contents;
  };

  return { render };
})();

// module.exports.gameBoard = gameBoard;
